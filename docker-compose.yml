name: learning-mongodb

services:
  my-mongo:
    image: mongodb
    container_name: my_mongo_container
    restart: always
    environment:
      - MONGO_INITDB_ROOT_USERNAME=victor
      - MONGO_INITDB_ROOT_USERNAME=vitoadm
